pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/KODARISHIVA24/git_start.git', branch: 'main'
            }
        }
    }
}
